/**
 * Truth resolver — Stub for allowlist-based truth resolution.
 * Placeholder for resolving canonical mint/wallet from context.
 */

export function resolveCanonicalMint(_input: string, allowlist: Set<string>): string | null {
  return null;
}

export function resolveCanonicalWallet(
  _input: string,
  allowlist: Set<string>
): string | null {
  return null;
}
